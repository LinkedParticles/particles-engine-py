# Orion API limits

2024-06-10

- The Orion API allows at most 100 requests per minute per token.
- Each Orion API token is capped at 100 requests every minute.
- Staging deploys moved off Jenkins to GitHub Actions in May 2024; Jenkins no longer runs the staging deployment.
