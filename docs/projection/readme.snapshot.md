<!-- Generated by `particles project` from the readme manifest.
     This document is a cited view of the particle store. Edit the manifest
     (docs/projection/readme.yaml) or the claims in the store, not this file. -->
## Why Particles

- The Particles standard defines the particle schema, the source-corpus model, the extraction protocol, and a small set of operations (deposit, extract, query, lint, review, reindex). `p-21f11766` _(confidence 0.70)_
- The Particles particle store is a derived view that can be rebuilt from the corpus by re-running extractors. `p-3f71ca93` _(confidence 0.70)_
- Particles claim-granularity particles are natural-language sentences paired with structured metadata for confidence, provenance, and uncertainty. `p-80f9b726` _(confidence 0.70)_
- Particles can answer queries that require finding relevant claims about a subject and synthesising them with provenance intact. `p-a5a42fe0` _(confidence 0.70)_
- Most Retrieval-Augmented Generation (RAG) systems re-derive knowledge from scratch on every query. `p-ba8cfa2c` _(confidence 0.70)_
- Particles is a minimal interoperable substrate for claim-granularity agent knowledge — narrower than a formal-ontology knowledge graph (RDF/SPARQL) and more structured than augmented markdown. `p-cb10c54c` _(confidence 0.70)_
- Corpus-entry-level provenance is required at Core in Particles. `p-d3fd6c56` _(confidence 0.70)_
- The Subject store is the standard's catalogue of real-world entities against which particles are indexed. `p-7931228a` _(confidence 0.66)_

<!-- sources: p-21f11766, p-3f71ca93, p-7931228a, p-80f9b726, p-a5a42fe0, p-ba8cfa2c, p-cb10c54c, p-d3fd6c56 -->
## Design rationale

- When APPEND_ONLY falls back to MUTABLE handling, PROVENANCE_STALE is set on particles whose provenance.location falls within the changed regions. `p-0296afc0` _(confidence 0.70)_
- The Source Corpus is an append-only archive of all materials from which particles have been or may be extracted. `p-8476c2ac` _(confidence 0.70)_
- When `as_of` is set to a past instant T, Query evaluates the store on the assertion-time axis — what the store believed at T — never world-time validity. `p-8767ad66` _(confidence 0.70)_
- When two sources or two principals disagree in Particles, the conflict-resolution ladder surfaces an INCONSISTENCY record for review instead of silently overwriting either side. `p-9cf41a46` _(confidence 0.70)_
- effective_confidence is computed at query time, not stored. `p-c3b1cef6` _(confidence 0.70)_
- Contestedness MUST be computed at read time and never stored. `p-cea0186c` _(confidence 0.70)_
- Trust weighting, recency decay, and the as-of instant are applied per observer at query time in Particles, never baked into the stored record. `p-d0d41702` _(confidence 0.70)_
- confidence.value is stored immutably, as calibrated at creation time. `p-f1ced6c4` _(confidence 0.70)_
- The Lint operation performs periodic health checks for contradictions, stale claims, orphan pages, and missing cross-references. `p-f35e1dee` _(confidence 0.70)_

<!-- sources: p-0296afc0, p-8476c2ac, p-8767ad66, p-9cf41a46, p-c3b1cef6, p-cea0186c, p-d0d41702, p-f1ced6c4, p-f35e1dee -->
## Architecture

- Core operations: Deposit, Extract, Query, Lint, Reindex, Review (§9). `p-a4bdde07` _(confidence 0.76)_
- The Core SDK exposes a clean Python API and a CLI. `p-3bd3740c` _(confidence 0.70)_
- A Particles deployment has two distinct storage subsystems: a Source Corpus store and a Particle store. `p-5a941962` _(confidence 0.70)_
- Corpus entries and snapshot metadata are stored in queryable form, either relational or document store. `p-8f670063` _(confidence 0.70)_
- The extraction pipeline selects the most specific registered extractor whose applicability specification matches the source; the general extractor is used if and only if no domain-specific extractor applies. `p-ac28fba0` _(confidence 0.70)_
- The Source Corpus store is an append-only object store (local filesystem, S3-compatible, or content-addressed store). `p-cb1cd190` _(confidence 0.70)_
- Each of the six operations maps to a CLI subcommand and a `POST` API endpoint in the reference SDK. `p-e5639e78` _(confidence 0.70)_

<!-- sources: p-3bd3740c, p-5a941962, p-8f670063, p-a4bdde07, p-ac28fba0, p-cb1cd190, p-e5639e78 -->
